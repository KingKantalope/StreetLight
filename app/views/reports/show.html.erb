<!DOCTYPE html>

<div class="container-sm">
  <div class="row">
    <center><h1>Incident Report</h1></center>
  </div>
  <hr class="bg-success border-2 border-top border-success">
  <div class="row">
    <h3><strong><%= @report.time_occur.try(:strftime, '%m/%d/%Y') %></strong></h3>
  </div>
  <div class="row">
    <h3><strong><%= @report.location %></strong></h3>
  </div>
  <br/>
  <div class="row">
    <!--Separate each tag and give individual border rather than being single list-->
    Tags:
    <%= @report.tags %>
  </div>
  <br/>
  <div class="row">
    <div class="card bg-dark bg-opacity-75 border-success">
      <div class="card-body">
        <p class="card-text">
          <%= @report.description %>
        </p>
      </div>
    </div>
  </div>
  <hr class="bg-success border-2 border-top border-success">
  <p>
  <%= image_tag "https://maps.googleapis.com/maps/api/staticmap?center=#{@report.latitude},#{@report.longitude}&zoom=17&size=500x400&key=#{Rails.application.credentials.google_maps_api_key}&signature=StreetLight", alt: "Map" %>
  </p>
  <!-- if user == report.user -->
  <hr class="bg-success border-2 border-top border-success">
</div>

<div class="row">
  <br/>
  <div class="col-3">
    <div class="d-grid gap-2">
      <%= link_to "Back", reports_path, class:"btn btn-outline-success btn-lg" %>
    </div>
  </div>
  <div class="col-6">
    <div class="d-grid gap-2">
      <%= link_to "Edit", edit_report_path(@report), class:"btn btn-outline-success btn-lg" %>
    </div>
  </div>
  <div class="col-3">
    <div class="d-grid gap-2 justify-content-end">
      <%= button_to "Delete", @report, method: :delete, class:"btn btn-outline-danger btn-lg" %>
    </div>
  </div>
  <br/><br/>
</div>
